# Environment Variables for User Management API

Create a `.env` file in the root of your project with the following content, replacing the values with your PostgreSQL credentials:

```
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_db_name
PORT=3000
# Example valuess
```
